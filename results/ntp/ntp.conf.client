tinker panic 0  # do not panic even if the clock jump would be giant
tinker minpoll 4  # 2^minpoll seconds; the lower bound
tinker maxpoll 4  # 2^maxpoll seconds

server 10.0.0.1 minpoll 4 maxpoll 4
#iburst
#broadcastclient

driftfile /root/ntpd.drift

# vim: set ft=conf:
