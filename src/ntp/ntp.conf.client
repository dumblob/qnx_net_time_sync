tinker panic 0  # do not panic even if the clock jump would be giant
tinker minpoll 0  # 2^minpoll seconds; the lower bound (maxpoll can't be set :()
#FIXME
tinker maxpoll 1  # 2^maxpoll seconds

server 10.0.0.1 iburst  # iburst ~ the most promising one

# vim: set ft=conf:
